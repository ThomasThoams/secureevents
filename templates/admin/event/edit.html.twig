{% extends 'base.html.twig' %}

{% block title %}Modifier {{ event.titre }} — Administration{% endblock %}

{% block body %}
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_admin_index') }}">Administration</a></li>
            <li class="breadcrumb-item active">Modifier</li>
        </ol>
    </nav>

    <h2 class="mb-4">Modifier : {{ event.titre }}</h2>

    <div class="card shadow-sm">
        <div class="card-body">
            {% include 'admin/event/_form.html.twig' %}
        </div>
    </div>
{% endblock %}
